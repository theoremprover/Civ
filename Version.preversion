module Version where

import Prelude

import Paths_Civ (version)
import Data.Version (showVersion)

compilationDateString :: String
compilationDateString = "<compilationDateString>"

gitHash :: String
gitHash = "<gitHash>"

cabalVersion :: String
cabalVersion = showVersion version
